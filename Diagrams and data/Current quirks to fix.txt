text on login screen should be INSIDE the fields
Create account request should be made once we have notifications on
User should be warned if they have the wrong username or password. Not just the console